<div>
	<h2>Restaurant: {{restaurant.name}}</h2>
    <h4>Cusine: {{restaurant.cuisine}}</h4>
    <h4>Location: {{restaurant.location}}</h4>
	<img class="item-img" src="{{restaurant.image}}" />
    <a class="btn" href="/menu/{{id}}">See restaurant's menu</a>
	<a class="btn" href="/restaurants">Back to Restaurant List</a>
</div>